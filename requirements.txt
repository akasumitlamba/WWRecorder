# WWRecorder — Python Dependencies
# Install with:  pip install -r requirements.txt

# ── GUI Framework ─────────────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── Screen Capture ────────────────────────────────────────────────────────────
mss>=9.0.1
numpy>=1.26.0           # Used for fast BGR conversion in frame worker
Pillow>=10.0.0          # Screenshot PNG export
pywin32>=306            # Added for native Windows cursor pulling

# ── Audio (Windows WASAPI loopback + Microphone) ─────────────────────────────
soundcard>=0.4.3        # WASAPI loopback and microphone via Windows MediaFoundation

# ── Global Hotkeys ────────────────────────────────────────────────────────────
pynput>=1.7.6

# ── Packaging ─────────────────────────────────────────────────────────────────
pyinstaller>=6.3.0      # Build: pyinstaller wwrecorder.spec
